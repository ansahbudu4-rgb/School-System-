cd "D:\New folder\School someting someting"

git init
git add .
git commit -m "Initial school system application"
git branch -M main
git remote add origin <<https://github.com/School-systemcd> "D:\New folder\School someting someting"

git init
git add .
git commit -m "Initial school system application"
git branch -M main
git remote add origin <https://github.com/School-system.git>
git push -u origin main.git>
git push -u origin main
