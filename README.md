# SchoolDesk

A school records workspace for managing admitted students and examination results.

## Run locally

Node.js 18+ is required.

```bash
npm install
npm run dev
```

The app uses local browser storage for this prototype, so new admissions and results remain available after refresh on the same device.
